BUGG | OSV
Bugg med att fråga inte sparas första gången.


FRÅGOR | FUNDERINGAR
Hur visa data från firebase? CHECK
Hur Update/Delete av frågor på bra sätt?
Måste markera vilket svar som är rätt. Hur? Radiobutton?
Hur spela spel? Visa en fråga i taget?
Rätta spel.


Play game: knapp för att hämta data från firebase, event once - laddar ner frågor
Då finns frågor 
Rendera playknapp för att spela

knapp för att ladda ner frågor med loadquestions
klick loadQuestions 
knapp ladda frågor om state är tom
play --> guess question  

lodash för isempty _.isEmpty(this.state.questions)


Om det inte finns frågor i state --> rendera knapp för att ladda (loadQuestions) CHECK
när det finns frågor i state --> rendera knapp för att spela (köra guessQuestion) CHECK




{_.map(this.state.questions,function(q){
		          	return <GuessQuestion data={q}/>;
		          })}



Slumpa fråga: 
{a:"bla",b:"tjo"}
Object.keys(myObj); // ["a","b"]

lodash: _.keys(myObj)
_.shuffle(_.keys(myObj))
var questions = _.first(_.shuffle(_.keys(myObj)),7);


räknare rätt i playgame
{questions:{},correctAnswers:0};
registerCorrectAnswer: function(){ this.setState({correctAnswers:this.state.correctAnswers+1}); }